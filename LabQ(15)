#include <stdio.h>

int main()
{
    int n;
    printf("Enter the number :\n");
    scanf("%d", &n);
    if ((n / 2) * 2 == n)
        printf("Number is even\n");
    else
        printf("Number is odd \n");
    return 0;
}
